<table>
  <th> Project ID </th>
  <th> Name </th>
  <% @projects.each do |project| %>
  <tr>
    <td> <%= project['id'] %></td>
    <td><%= link_to "#{project['name']}", 'https://app.procore.com/' + "#{project['id']}" + '/project/home', :target => "_blank" %></td>
  </tr>
  <% end %>
</table>
